export default function DashboardPage() {
  return (
    <div className="page">
      <h2>Dashboard</h2>
      <p className="text-muted">
        Select an entity from the sidebar, or use the terminal below.
      </p>
    </div>
  );
}
